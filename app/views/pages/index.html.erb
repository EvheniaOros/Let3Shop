<h1>Pages#index</h1>
<p>Find me in app/views/pages/index.html.erb</p>
<%= link_to "Login", new_user_session_path %><br />
<%= link_to "Logout", destroy_user_session_path, method: :destroy %><br />
<%= link_to "Create new post", new_post_path %>

	<p>Create new post</p>
	
		<% @posts.each do |post|%>
		<p>User <%= post.title %> write: %>
		<b><%= post.description %></b><br />
		<%= post.price %></p>
		<p><%= link_to "Read More", post_path(post.id) %></p>
	<% end %>
<% end %>

